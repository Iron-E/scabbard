import { describe, expect, it } from 'vitest';
import { Provider } from './state';

describe(Provider, () => {
	describe.todo(Provider.prototype.provide, () => {
		it.todo('stores state', () => {

		});

		it.todo('stores dependencies', () => {
		});
	});

	describe.todo(Provider.prototype.prepare, () => {
	});

	describe.todo(Provider.prototype.injector, () => {
	});
});
